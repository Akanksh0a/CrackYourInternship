class Solution {
public:
    int getDecimalValue(ListNode* head) {
        ListNode* temp=head;
        int sum=0;
        while(temp!=NULL)
        {
            sum=2*sum+temp->val;
            temp=temp->next;
        }
        return sum;
    }
};
